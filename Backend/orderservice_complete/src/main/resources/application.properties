# Application name
spring.application.name=Order-service

# Server port
server.port=9090

# Datasource configuration
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate configuration
spring.jpa.hibernate.ddl-auto=update

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway configuration
spring.flyway.enabled=false

# Eureka client configuration
eureka.client.service-url.defaultZone=http://localhost:8761/eureka


jwt.secret=iTx2Ep09WzU3PhkSVCHLvMon61ecZrqmfAjGN4JKt7Fulbg8IydsDa5OXYwQRB
jwt.expiration=3600000


# User Profile Service URL
services.user-profile.url=http://localhost:8082


services.PRODUCT-SERVICE.url=http://localhost:8080

logging.level.org.springframework.cloud.loadbalancer=DEBUG
logging.level.feign=DEBUG



# =========================
# HIBERNATE SQL + BINDING
# =========================
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# =========================
# JPA / TRANSACTION ERRORS
# =========================
logging.level.org.springframework.orm.jpa=DEBUG
logging.level.org.springframework.transaction=TRACE

# =========================
# JDBC / POSTGRES ERRORS
# =========================
logging.level.org.hibernate.engine.jdbc.spi.SqlExceptionHelper=ERROR

server.error.include-message=always
server.error.include-binding-errors=always
server.error.include-exception=true


