spring:
  application:
    name: coupon-service

  datasource:
    url: jdbc:postgresql://ep-odd-bush-a4wqa81j-pooler.us-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_Lz1QVypvwqT8
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        default_schema: public   # ✅ IMPORTANT FIX

  flyway:
    enabled: false   # ✅ Required for PostgreSQL 17

server:
  port: 8060

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
